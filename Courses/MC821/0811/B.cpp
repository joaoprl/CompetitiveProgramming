
#include <iostream>

using namespace std;

int main(){
  ios::sync_with_stdio(false);
  int m, n;
  while(cin >> m >> n && (m != 0 || n != 0)
    int *f = new int[m + n + 1];
    for(int i = 0; i <= m + n; i++)
      cin >> f[i];

    // solve linear system (?)
  }

  return 0;
}
