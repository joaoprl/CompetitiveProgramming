
#include <iostream>
#include <set>

#define N 10010

using namespace std;

int main(){
  ios::sync_with_stdio(false);

  int *seq = new int[N];

  int n;
  while(cin >> n && n != -1){
    for(int i = 1; i <= n; i++)
      cin >> seq[i];

    
  }

  return 0;
}
